--- ext/CMakeLists.txt.orig	2026-05-14 00:42:37 UTC
+++ ext/CMakeLists.txt
@@ -60,6 +60,10 @@ endif()
 	add_subdirectory(libchdr-build)
 endif()
 
+if(BUILD_BUNDLED_FFMPEG)
+	add_subdirectory(ffmpeg-build)
+endif()
+
 if(ANDROID)
     if (ARM64)
         add_subdirectory(libadrenotools)
